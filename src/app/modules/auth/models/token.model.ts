
export interface Token {
  accessToken: string;
  tokenType: string;
  expiresIn: string;
  refreshToken: string;
  clientId: string;
  userId: string;
  issued: Date;
  expires: string;
  sessionId: string;
}
