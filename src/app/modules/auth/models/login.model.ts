export interface LoginModel {
  userName: string;
  password: string;
}

export interface LoginSignModel {
  keyId: string;
  buffer: string;
  sign: string;
  moduleName: string;
  tokenId: string;
}
